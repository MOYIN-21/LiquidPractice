HI
{% if product.title == "The Collection Snowboard: Liquid" %}
  <h1>YES, This is the best snowboard yet</h1>
{% else %}
  <h1>Nope, not the best snowboard</h1>
{% endif %}
  <h1>{{ product.price }}</h1>


{% assign variable_name = "God" %}
{{ variable_name | upcase | remove: "O" }}

{% capture variable %}
  <p>value</p>
{% endcapture %}
{{ variable | strip_html }}

{% assign array = "one,two,three,four" | split: "," %}
{% for item in array %}
  <p>{{ item }}</p>
{% endfor %}

{% # dataTypes %}
{% assign string = "This is a string" %}
{% assign number = 42 %}
{% assign boolean = true %}
{% assign array = "apple,banana,cherry" | split: "," %}
{% assign object = '{"name": "John", "age": 30}' | parse_json %}
{% assign nil_value = nil %}
{{ string }}<br>
{{ number }}<br>
{{ boolean }}<br>
{% for fruit in array %}
  {{ fruit }}<br>
{% endfor %}
{{ object.name }} is {{ object.age }} years old.<br>
{{ nil_value | default: "This is a default value for nil" }}<br>

{% assign products = collections.all.products %}
{% for product in products %}
  <h2>{{ product.title }}</h2>
  <p>Price: {{ product.price | money }}</p>
  {% if product.compare_at_price > product.price %}
    <p>On Sale! Was: {{ product.compare_at_price | money }}</p>
  {% endif %}
  {% if product.available %}
    <p>Status: In Stock</p>
  {% else %}
    <p>Status: Out of Stock</p>
  {% endif %}
{% endfor %}

{% assign current_time = 'now' | date: "%Y-%m-%d %H:%M:%S" %}